# Image Resize - Plugin for IO CMS

```php
$image = new \Gumlet\ImageResize($from);
$image->quality_jpg = 90;
$image->resizeToWidth(300);
$image->save($to);
```

## Libraries used

- https://github.com/gumlet/php-image-resize